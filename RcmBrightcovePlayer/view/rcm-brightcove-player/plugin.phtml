<?php

$this->headScript()->appendFile(
    $this->basePath() . '/modules/rcm-brightcove-player/jquery-model/jquery.brightcove.js',
    'text/javascript'
);

$this->headScript()->appendFile(
    $this->basePath() . '/modules/rcm-brightcove-player/rcm-brightcove-player.js',
    'text/javascript'
);

//$this->headLink()->appendStylesheet(
//    $this->basePath() . '/modules/rcm-brightcove-player/style.css'
//);

?>

<div style="display:none;">
    <form id="brightcoveform" title="Brightcove Player Editor">

        <div  ng-controller="BrightcoveCtrl">
            <div>
                <div ng-show="true">
                    <select
                        ng-options="ng-options="item as item.name for item in items">
<!--                        <option value="single">Single Embed</option>-->
<!--                        <option value="multiple">Muliple video player</option>-->
                        <option>--</option>
                    </select>
                </div>
<!--                <span ng-if="true" ng-hide="false">-->
                <div ng-hide="true">
                    Choose video:<br>
                    <select  data-ng-options="video.name for video in videos" data-ng-model="selectedVideos">
                    </select>
                </div>
<!--                </span>-->
                <div><br></div>
                <div ng-hide="true">
                    Choose Playlist:<br>
                    <select class="multiselect" multiple="multiple" data-ng-options="playlist.name for playlist in playlists" data-ng-model="selectedPlaylists" multiselect-dropdown>
                    </select>
                </div>
                <div><br></div>
            </div>
        </div>
    </form>
</div>

