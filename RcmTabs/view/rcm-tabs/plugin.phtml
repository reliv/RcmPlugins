<div class="tabs" id="rcmTabs_<?= $this->instanceId; ?>">
    <ul class="titleWrap">
        <?php foreach ($this->ic['containers'] as $tab) {
            $tabId = $tab['id'];
            $title = $this->ic['tab_title_' . $tabId];
            include 'tab-title.phtml';
        } ?>
    </ul>
    <div class="bodyWrap">
        <?php foreach ($this->ic['containers'] as $tab) {
            $tabId = $tab['id'];
            $content = $this->ic['tab_content_' . $tabId];
            include 'tab-body.phtml';
        } ?>
    </div>
</div>

<script type="text/javascript">
    $(function () {
        $('#rcmTabs_<?=  $this->instanceId; ?>').tabs();
    })
</script>
